---
import FormattedDate from '../components/FormattedDate.astro';
import { Image } from 'astro:assets';


const {img="", title="Awesome Blog Title", date="2/28/1998", tags=["Tech", "OSINT"], link="#"} = Astro.props;
---

<div class="w-full bg-black  mx-auto border-2 border-primary rounded-2xl overflow-hidden p-2 pb-6">
  <a href={link} aria-label={`An image of ${title} blog`}>
  <div class="grid w-full rounded-t-xl overflow-hidden relative">
      <Image src={img} width={"100"} height={'100'} alt={img} loading="lazy" class="w-full" />
  </div>

  <div class="flex flex-col justify-center">
    <div class="mx-4 flex flex-col gap-2 place-items-start">
        <p class="text-lg md:text-xl text-pretty mt-4 font-light text-gray-400 date">
          <FormattedDate date={date} />
        </p>
        <p class="text-2xl md:text-3xl mb-4 font-medium">
          {title}
        </p>
    </div>
    <div class="w-full border rounded-full border-primary-light mx-auto"></div>
    <p class="m-4 mb-2 font-light text-lg">Tags:</p>
    <div class="flex flex-wrap gap-4 mx-4 text-primary-light text-nowrap">
      {tags.map((tag) => (
      <p>{tag}</p>
      ))}
    </div>
  </div>
</a>
</div>
