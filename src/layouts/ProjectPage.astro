---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Nav from '../components/Nav.jsx'
import FormattedDate from '../components/FormattedDate.astro';
import GridBackgroundContainer from '../components/HexGridContainer/HexBackgroundContainer.jsx'
import "../styles/global.css"

type Props = CollectionEntry<'projects'>['data'];

const { title, description, image, webLink, githubLink, tags } = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={description} />
	</head>

  <body class="flex flex-col place-items-center">
    <Nav client:load />
    <main class="text-white w-full min-h-screen md:w-10/12 lg:w-8/12 blog-bg pb-10">
			<article>
				<div class="hero-image flex justify-center">
					{image && <Image width={1020} height={510} src={image} alt="" />}
				</div>
				<div class="prose prose-invert prose-img:mx-0 prose-img:w-full max-w-none px-4 lg:px-6">


					<div class="title pt-8">
						<h1>{title}</h1>
					</div>


					<slot />
				</div>
			</article>
    </main>
    <GridBackgroundContainer client:load hexSize='700px' lightOutside="#3df03a" hexBrightness="15%" width="w-screen" >
    </GridBackgroundContainer>
    <Footer />
	</body>
</html>

